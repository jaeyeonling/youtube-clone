extends layouts/main
include mixins/videoBlock

block content
  .home-videos
    each video in videos
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)
      +videoBlock(video)