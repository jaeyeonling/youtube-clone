mixin videoPlayer(video = {})
  .videoPlayer
    video(src=`/${video.src}`, controls=true)
      .videoPlayer__controls
        .videoPlayer__column
          span
            i.fas.fa-volume-up
        .videoPlayer__column
          span
            i.fas.fa-play
        .videoPlayer__column
          span
            i.fas.fa-expand